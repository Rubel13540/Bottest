{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt || python3 -m pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python bot.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
