‎services:
‎  - type: worker
‎    name: telegram-url-bot
‎    env: python
‎    buildCommand: pip install -r requirements.txt && playwright install chromium
‎    startCommand: python bot.py
‎    envVars:
‎      - key: TELEGRAM_BOT_TOKEN
‎        sync: false
‎
